# 在线协作文档编辑系统

## 项目简介
一个支持多人实时协同的在线文档编辑平台（类似简化版 Google Docs）。用户可在同一篇文档中同时进行编辑，系统实时同步所有协作者的修改，并提供完整的撤销/重做及操作历史追溯能力。

## 应用场景
- **团队会议记录**：多人同时整理会议纪要，实时看到他人编辑内容。
- **远程协作撰稿**：多位作者共同编写方案、报告或技术文档。
- **教学/演示**：教师编辑示范文档，学生实时观看并参与修改。

## 核心设计模式及运用

### 1. 命令模式的取舍（最终未采用）
最初的设计是把每次编辑封装成 `Command`（`execute()` / `undo()`），服务端执行命令并回滚。实现后发现前提不
成立：权威内容是 TipTap/ProseMirror 文档 JSON，而命令要作用在"字符偏移"上才能算出插入/删除位置，对富文本
是结构性错误（详见"为什么不用字符偏移 OT 处理富文本"）。因此 `pattern/command` 与 `ot/` 已删除，改由
**服务端定序 + 客户端 step 变换**承担同一职责：

- 封装每次编辑的是客户端的 ProseMirror step 批次（不透明 JSON），不再是服务端的命令对象；
- "执行"是行锁下的版本分配与 CAS，"回滚"是 `document_snapshot` 的恢复（写新版本，绝不回退序列号）；
- 撤销/重做由编辑器本地的 ProseMirror history 承担，天然按用户隔离，不需要服务端命令历史栈。

### 2. 备忘录模式（Memento Pattern）
**职责**：在每次执行命令前，保存文档的当前状态快照（备忘录），由专门的负责人（Caretaker）管理历史栈。

**优势**：
- 执行 `undo()` 时，可直接从备忘录恢复文档到上一个状态，而不暴露文档内部结构。
- 支持多步撤销/重做，且备忘录可持久化到数据库，用于操作历史追溯与灾难恢复。
- 文档对象封装性不受破坏，状态管理职责清晰。

### 3. 观察者模式（Observer Pattern）
**职责**：文档作为被观察者（Subject），所有在线协作用户作为观察者（Observer）。

**优势**：
- 当任一用户执行编辑命令导致文档内容变化时，文档自动通知所有订阅该文档的观察者。
- 观察者收到通知后拉取最新文档内容，更新自己的 Web 页面，从而实现实时协同效果。
- 观察者可动态加入或退出，天然适配用户进入/离开编辑会话的场景。

## 协同冲突处理机制

多个用户可能同时编辑同一文档，系统必须保证各客户端最终内容一致（convergence）、且不丢失任何人的输入
（intention preservation）。本节记录实际实现的方案，以及为什么没有采用最初设想的字符偏移 OT。

### 实际方案：服务端定序 + 客户端变换（sequencer / step-log）

服务端**不解释文档内容**，只负责给操作排序：

1. 客户端把本地编辑打包成 ProseMirror **step 批次**（对服务端是一串不透明 JSON），连同自己依据的
   `baseVersion` 一起发出。
2. 服务端对该文档行加 `PESSIMISTIC_WRITE` 锁，校验 `baseVersion` 等于锁到的当前 `version`，
   分配 `seq = version + 1`，以 CAS（`update ... where version = :base`）写入，落 `operation_log`。
3. 事务提交后，向该文档的每个会话广播一条带 `seq` 的 `STEPS` 帧；**发送者本人被排除**。
4. `baseVersion` 过期时返回 `REJECT{version}`，客户端补齐缺的操作、重映射自己的 step 后重发。
5. 每 `CHECKPOINT_EVERY`（200）个版本，服务端通过 `ACK.checkpointRequested` 请某个已确认的客户端上传
   整篇文档 JSON 作为检查点，检查点落库后折叠其下方的日志。

因此一致性由**客户端库**（`frontend/src/collab/otClient.js`）保证：它维持至多一个未确认批次，并把
到达的远端 step 与本地未确认 step **双向映射**——远端 step 映射到本地文档坐标系才能正确落地，本地 step
映射到服务端顺序才能继续发送。双向都缺一不可。

### 为什么不用字符偏移 OT 处理富文本

最初设想（见"预期成果"与旧版流程图）是在纯文本上用经典 OT：`INSERT(pos, text)` / `DELETE(pos, len)`
在服务端两两转换。实现后放弃，原因是**坐标系不一致**：

- 权威内容是 TipTap 富文本 JSON，内含段落、标记（bold/code 等）与嵌套结构；
- 而 `pos` 是纯文本字符下标。同一批编辑在"字符串坐标系"与"文档树坐标系"里的位置并不对应；
- 要让偏移 OT 成立，服务端必须成为内容的解释者，即用 Java 重写一套 ProseMirror 的
  `ReplaceStep`/mark map 逻辑——成本高且必然与客户端实现漂移。

也就是说，偏移 OT 用在富文本上不是"精度不够"，而是**结构性错误**。所以 `ot/OperationalTransform`、
`ot/OTOperation` 与 `pattern/command` 中的插入/删除命令曾作为纯文本层面的实现示例保留，现已**删除**：
它们既不在写入主链路上，也没有别的调用方，留着只会让人误以为服务端会解释文档内容。

### 与 CRDT 的关系

CRDT（如 Yjs + `@tiptap/extension-collaboration`）是本项目的演进方向，优势是天然去中心化、抗弱网、
支持离线合并。当前实现选择了定序模型，理由：复用既有的 `document.version` 版本链与 `operation_log`
审计链，服务端零内容依赖（不需要引入 PM 运行时），并且一致性推理可以在面试/答辩中逐步演示。
代价是服务端必须在线定序，且断线期间的并发输入依赖客户端重放。

**已知不保证项**：同一段文本内"同一字符"的真同时输入（服务端看不到已断开标签页未提交的 delta）；
传输帧本身的到达顺序（帧携带 `seq`，客户端遇空洞回补）；日志被裁剪到低于某客户端已知版本之后的全量一致；
以及任何恶意客户端——身份目前仍是客户端自报的 `userId`。

## 功能特性

| 功能 | 说明 |
|------|------|
| 实时协同编辑 | 多人同时编辑同一文档，以 step 批次增量同步，非整篇覆盖 |
| 冲突解决 | 服务端定序 + 客户端双向 step 映射；基版本过期走 REJECT→补齐→重发，不丢输入 |
| 操作撤销/重做 | 编辑器内按用户独立撤销（ProseMirror history），不会撤掉别人的输入；服务端提供版本恢复 |
| 操作历史追溯 | `operation_log` 记录每个版本的操作与作者，可查看并按检查点版本恢复 |
| 在线人数与协作者 | 会话数实时展示，协作者名字标签 + 正文内远端光标 |
| 文档分享与权限 | OWNER / READ_WRITE / READ_ONLY 三档，服务端强制，只读端编辑器不可输入 |
| 分享通知 | 站内通知落库（离线可见）+ 通过 WebSocket 实时推送到该用户所有会话 |
| 文档管理 | 标题重命名、按标题搜索我的与共享给我的文档、深链 `/#/doc/:id` |
| 版本与恢复 | `document.version` 单调递增；恢复历史版本以"新版本号携带旧内容"实现 |
| Web 可视化编辑器 | TipTap 富文本（粗体/斜体/删除线/行内代码等），支持导出 .doc |
| 前后端分离 | RESTful API + WebSocket，Vite 代理 `/api` 与 `/ws` |

未实现（后续计划）：站内富文本@提及、图片上传、PDF/Markdown 导出、离线编辑与本地持久队列、登录接口的
限流与失败锁定。已完成但**未做双实例运行时验证**的是协同状态外置（`CollabBus` + `COLLAB_BUS=redis`）。

## 技术选型

| 层级 | 技术 |
|------|------|
| 后端 | Java + Spring Boot |
| 数据库 | MySQL |
| 实时通信 | WebSocket |
| 协同算法 | 服务端定序 + ProseMirror step 变换（CRDT 为演进方向） |
| 前端 | Vue 3 + Vite + TipTap 3 |
| 架构 | 前后端分离，RESTful API + WebSocket |

## 系统架构概要

```
┌─────────────┐  WebSocket  ┌─────────────────┐  JDBC  ┌─────────────┐
│  前端编辑器   │◄──────────►│   后端服务        │◄─────►│   数据库     │
│  (Vue/React) │            │  (Spring Boot)   │       │  MySQL      │
└─────────────┘            └─────────────────┘       └─────────────┘
       │                          │
       │  RESTful API             │  定序(CAS) + 备忘录 + 观察者
       │◄────────────────────────►│  + 观察者模式
       │                          │
       │                          │  服务端定序 + 客户端 step 变换
       └──────────────────────────┘
```

**流程说明**：
1. 前端打开 `/#/doc/:id` 时建立 WebSocket 长连接。浏览器无法给 WebSocket 带 `Authorization` 头，所以令牌走
   查询串 `?token=`：握手拦截器先验令牌（无有效令牌直接 401 拒绝升级），文档 ID 取自有权限校验的路径段，
   查询串里的 `userId` 不再被读取。升级成功后若账号被停用或权限被收回，处理器以 `4001` 关闭。
2. 连接建立后服务端下发 `INIT`：标题、**最新检查点**内容及其 `checkpointVersion`、当前 `version`、在线人数、
   我的权限。客户端随后用 `GET /{id}/operations?after=checkpointVersion` 重放差量，才能得到最新文本——
   因为服务端不定期的内容只以操作形式存在。
3. 本地编辑被打包成 step 批次经 `STEP_BATCH` 发出；服务端在行锁下校验基版本、分配 `seq`、写
   `operation_log`，提交后广播 `STEPS`（排除发送者）并单发给对方 `ACK`。
4. 基版本过期时服务端回 `REJECT{version}`，客户端补齐缺失操作、双向重映射后重发；帧乱序或丢失由 `seq`
   空洞检测 + 回补兜底；任何异常退化为整篇重取。
5. 观察者模式在此体现为"每文档一个观察者、一次广播"，并用引用计数与会话保持与断开；通知在事务提交后
   触发（`AFTER_COMMIT`），回滚的写不会到达任何客户端。
6. 每 200 个版本请求一次检查点上传（`POST /{id}/checkpoint`），落 `document_snapshot` 并折叠其下方的
   操作日志；快照每文档最多保留 50 份。恢复版本时以旧内容铸造新版本号并广播 `RESET`。

## 数据库设计

### document 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 文档主键 |
| title | VARCHAR(255) | 文档标题（可重命名） |
| content | LONGTEXT | 最新检查点内容（HTML 或 TipTap doc JSON） |
| content_format | VARCHAR(20) | `html` \| `doc-json` |
| version | INT | 协同序列号，只增不减，由服务端在行锁下 CAS 分配 |
| revision | BIGINT | JPA `@Version`，保护实体级更新；序列号以 `version` 为准 |
| created_by | BIGINT FK | 创建者用户ID |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后更新时间 |

### document_share 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 分享记录主键 |
| document_id | BIGINT FK | 被分享文档 |
| user_id | BIGINT FK | 被分享用户 |
| permission | VARCHAR(20) | `READ_WRITE` \| `READ_ONLY` |
| shared_by | BIGINT FK | 操作人（须为文档所有者） |
| created_at | DATETIME | 分享时间 |

唯一约束 `(document_id, user_id)`。

### notification 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 通知主键 |
| user_id | BIGINT FK | 接收者 |
| document_id | BIGINT | 关联文档，可空 |
| type | VARCHAR(30) | 目前为 `SHARE` |
| message | VARCHAR(255) | 展示文案 |
| read_flag | BOOLEAN | 是否已读 |
| created_at | DATETIME | 产生时间 |

分享时先落库再经 WebSocket 推送，因此接收者离线也能在下次登录后看到。

### user 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 用户主键 |
| user_code | VARCHAR(8) UK | 8 位数字分享码，用于把文档分享给别人 |
| username | VARCHAR(100) UK | 用户名 |
| email | VARCHAR(255) UK | 邮箱 |
| password_hash | VARCHAR(255) | 密码哈希（当前为无盐 SHA-256，待升级为 BCrypt） |
| role | VARCHAR(20) | `ADMIN` \| `USER`，首个管理员需直接写库 |
| enabled | BOOLEAN | 停用后登录返回 403 |
| created_at | DATETIME | 注册时间 |

### operation_log 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 操作记录主键 |
| document_id | BIGINT FK | 关联文档 |
| user_id | BIGINT FK | 操作用户 |
| command_type | VARCHAR(50) | `STEPS`（协同批次）\| `SAVE`（整篇写入）\| `RESTORE`（版本恢复） |
| command_params | JSON | `STEPS` 保存 `{baseVersion, clientId, docSize, steps[]}` |
| version | INT | 该操作对应的序列号 |
| created_at | DATETIME | 操作时间 |

唯一约束 `(document_id, version)`：一条序列只能有一条记录。

### document_snapshot 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 快照主键 |
| document_id | BIGINT FK | 关联文档 |
| content | LONGTEXT | 检查点内容 |
| content_format | VARCHAR(20) | 与文档一致：`html` \| `doc-json` |
| version | INT | 检查点所在序列号 |
| created_at | DATETIME | 快照时间 |

唯一约束 `(document_id, version)`；每文档最多保留 50 份，超出时裁剪最旧的。

> 说明：`backend/src/main/resources/schema.sql` 只是参考文档，实际表结构由 JPA 实体经
> `ddl-auto: update` 生成，两者需人工保持一致。

## 预期成果

构建一个可实际运行的在线协作文档编辑系统，具备完整的实时协同、并发冲突解决、版本恢复与操作历史记录功能，
并通过 Web 界面进行交互演示。验收方式：两个浏览器标签页以不同用户同时编辑同一文档，双方最终内容一致、
各自输入均保留、光标不跳，且 `operation_log` 中不存在同一文档重复序列号。

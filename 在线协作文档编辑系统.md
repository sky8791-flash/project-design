# 在线协作文档编辑系统

## 项目简介
一个支持多人实时协同的在线文档编辑平台（类似简化版 Google Docs）。用户可在同一篇文档中同时进行编辑，系统实时同步所有协作者的修改，并提供完整的撤销/重做及操作历史追溯能力。

## 应用场景
- **团队会议记录**：多人同时整理会议纪要，实时看到他人编辑内容。
- **远程协作撰稿**：多位作者共同编写方案、报告或技术文档。
- **教学/演示**：教师编辑示范文档，学生实时观看并参与修改。

## 核心设计模式及运用

### 1. 命令模式（Command Pattern）
**职责**：将用户的每次编辑操作（插入文字、删除文字、格式修改等）封装为独立的命令对象。

**优势**：
- 命令对象统一提供 `execute()` 与 `undo()` 方法，系统调用即可完成执行与回滚。
- 新增编辑操作类型时，只需增加新的命令类，无需修改已有逻辑，符合开闭原则。
- 可方便地实现操作队列、宏命令与操作日志持久化。

### 2. 备忘录模式（Memento Pattern）
**职责**：在每次执行命令前，保存文档的当前状态快照（备忘录），由专门的负责人（Caretaker）管理历史栈。

**优势**：
- 执行 `undo()` 时，可直接从备忘录恢复文档到上一个状态，而不暴露文档内部结构。
- 支持多步撤销/重做，且备忘录可持久化到数据库，用于操作历史追溯与灾难恢复。
- 文档对象封装性不受破坏，状态管理职责清晰。

### 3. 观察者模式（Observer Pattern）
**职责**：文档作为被观察者（Subject），所有在线协作用户作为观察者（Observer）。

**优势**：
- 当任一用户执行编辑命令导致文档内容变化时，文档自动通知所有订阅该文档的观察者。
- 观察者收到通知后拉取最新文档内容，更新自己的 Web 页面，从而实现实时协同效果。
- 观察者可动态加入或退出，天然适配用户进入/离开编辑会话的场景。

## 协同冲突处理机制

由于多个用户可能同时编辑文档的同一位置，系统需要解决并发写入的冲突问题，确保各客户端最终内容一致。为此，系统将集成以下两种主流算法之一（或混合方案）：

### OT（Operational Transformation）机制
- 对并发的编辑操作进行转换，使不同用户的操作在服务端合并为一致的顺序。
- 常用于中心化架构（如 Google Docs），所有操作通过服务端中转并转换。
- **优点**：逻辑相对集中，成熟方案多。
- **缺点**：需维护中心服务器，并发量极高时中心压力较大。

### CRDT（Conflict-free Replicated Data Type）机制
- 采用无冲突复制数据类型，保证多端操作无需中心协调即可实现最终一致。
- 数据结构设计保证并发操作的交换律、结合律和幂等性，任意顺序合并结果相同。
- **优点**：天然去中心化，抗网络延迟，适合分布式协同。
- **缺点**：数据结构设计复杂，某些场景下元数据开销较大。

**系统预设方案**：首版以中心化 OT 实现，服务端通过操作转换算法保证一致性，后期可演进为 CRDT 以适应更大规模、弱网环境下的协同需求。

## 功能特性

| 功能 | 说明 |
|------|------|
| 实时协同编辑 | 多人同时编辑同一文档，内容毫秒级同步 |
| 冲突智能解决 | 采用 OT/CRDT 算法确保多端编辑最终一致，无内容丢失 |
| 操作撤销/重做 | 支持多步撤销与重做，基于命令与备忘录实现 |
| 操作历史追溯 | 后台完整记录所有编辑操作，可查看历史版本和操作明细 |
| 在线人数展示 | 前端界面实时显示当前正在编辑的用户列表 |
| Web 可视化编辑器 | 提供简洁易用的浏览器端编辑器，支持常规文本编辑功能 |
| 前后端分离 | 前端通过 RESTful API 与 WebSocket 交互，支持独立开发与部署 |

## 技术选型

| 层级 | 技术 |
|------|------|
| 后端 | Java + Spring Boot |
| 数据库 | MySQL |
| 实时通信 | WebSocket |
| 协同算法 | OT（初期）/ CRDT（演进方向） |
| 前端 | Vue.js / React |
| 架构 | 前后端分离，RESTful API + WebSocket |

## 系统架构概要

```
┌─────────────┐  WebSocket  ┌─────────────────┐  JDBC  ┌─────────────┐
│  前端编辑器   │◄──────────►│   后端服务        │◄─────►│   数据库     │
│  (Vue/React) │            │  (Spring Boot)   │       │  MySQL      │
└─────────────┘            └─────────────────┘       └─────────────┘
       │                          │
       │  RESTful API             │  命令模式 + 备忘录模式
       │◄────────────────────────►│  + 观察者模式
       │                          │
       │                          │  OT/CRDT 冲突解决
       └──────────────────────────┘
```

**流程说明**：
1. 前端编辑器通过 WebSocket 与后端维持长连接，用于接收文档更新通知和广播本地编辑操作。
2. 后端将编辑操作封装为命令对象，执行命令前通过备忘录保存快照，执行命令后更新文档状态。
3. 针对并发操作，服务端基于 OT（或 CRDT）算法对操作进行冲突检测与转换，保证一致性。
4. 文档变更后触发观察者通知，由服务端将转换后的最新内容或差异推送给所有协作者。
5. 所有命令与备忘录持久化至数据库，用于撤销/重做及历史审计。

## 数据库设计

### document 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 文档主键 |
| title | VARCHAR(255) | 文档标题 |
| content | LONGTEXT | 文档最新内容 |
| created_by | BIGINT FK | 创建者用户ID |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 最后更新时间 |

### user 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 用户主键 |
| username | VARCHAR(100) | 用户名 |
| email | VARCHAR(255) | 邮箱 |
| password_hash | VARCHAR(255) | 密码哈希 |
| created_at | DATETIME | 注册时间 |

### operation_log 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 操作记录主键 |
| document_id | BIGINT FK | 关联文档 |
| user_id | BIGINT FK | 操作用户 |
| command_type | VARCHAR(50) | 命令类型（INSERT/DELETE/FORMAT等） |
| command_params | JSON | 命令参数 |
| version | INT | 操作版本号 |
| created_at | DATETIME | 操作时间 |

### document_snapshot 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 快照主键 |
| document_id | BIGINT FK | 关联文档 |
| content | LONGTEXT | 快照内容 |
| version | INT | 版本号 |
| created_at | DATETIME | 快照时间 |

## 预期成果

构建一个可实际运行的在线协作文档编辑系统，具备完整的实时协同、智能冲突解决、撤销重做和操作历史记录功能，并通过 Web 界面进行交互演示。

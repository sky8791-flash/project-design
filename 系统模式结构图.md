# 系统模式结构图

## 整体架构图

```mermaid
graph TB
    subgraph "前端层"
        A1[用户界面] --> A2[编辑器组件]
        A2 --> A3[WebSocket客户端]
    end
    
    subgraph "后端服务层"
        B1[DocumentWebSocketHandler<br>身份来自握手 token] --> B2[DocumentService<br>定序器]
        B0[REST 控制器<br>文档/用户/通知/管理] --> B2
        B2 --> B3[行锁 PESSIMISTIC_WRITE<br>+ version CAS]
        B2 --> B4[CollabBus<br>memory 或 redis]
    end
    
    subgraph "数据层"
        C1[(MySQL<br>document / operation_log / document_snapshot)]
        C2[(Redis<br>仅 COLLAB_BUS=redis)]
    end
    
    A3 <-->|WebSocket| B1
    A2 <-->|REST| B0
    B3 --> C1
    B4 -.-> C2
    B4 -->|本节点投递| A3
    
    style A1 fill:#e1f5fe
    style B2 fill:#f3e5f5
    style C1 fill:#e8f5e8
```

服务端不解释文档内容：客户端发不透明的 ProseMirror step 批次，服务端只负责排序与鉴权，合流由客户端
`otClient.js` 完成。图中没有"命令调度器"和"OT/CRDT 引擎"，因为这两层从未实现为服务端组件
（理由见设计文档）。

## 设计模式协作图

```mermaid
classDiagram
    class DocumentMemento {
        -content: String
        -contentFormat: String
        -version: int
        +getContent()
        +getVersion()
    }
    
    class MementoCaretaker {
        -snapshots: Map~Long, List~DocumentMemento~~
        -MAX_SNAPSHOTS: int = 50
        +capture(documentId, content, format, version)
        +restore(documentId, version) DocumentMemento
    }
    
    class DocumentSubject {
        <<interface>>
        +attach(observer)
        +detach(documentId)
        +notifyAllObservers(event)
    }
    
    class DocumentSubjectImpl {
        -registrations: Map~String, Registration~
        +attach(observer)
        +detach(documentId)
        +notifyAllObservers(event)
    }
    
    class DocumentObserver {
        <<interface>>
        +update(ContentAppliedEvent)
        +getDocumentId()
    }
    
    class WebSocketObserver {
        -bus: CollabBus
        -documentId: String
        +update(event)
    }
    
    class DocumentService {
        -documentRepository: DocumentRepository
        -bus: CollabBus
        +appendStepBatch()
        +restoreVersion()
        +recordCheckpoint()
    }
    
    DocumentSubjectImpl o-- DocumentObserver : 每文档一个
    DocumentObserver <|.. WebSocketObserver
    DocumentService --> MementoCaretaker : 检查点与恢复
    MementoCaretaker o-- DocumentMemento : 每文档至多 50 份
    WebSocketObserver --> CollabBus : 投递
    DocumentService --> ContentAppliedEvent : 提交后发布
    DocumentService ..> DocumentSubjectImpl : 经事件触发
    DocumentSubjectImpl ..> ContentAppliedEvent : AFTER_COMMIT
```

实现说明：`DocumentService.restoreVersion` 是备忘录模式的真实调用方；编辑器内的撤销/重做是前端
ProseMirror history，不经过服务端。`pattern/memento`、`pattern/observer` 是仅存的两个模式包 ——
原先的 `pattern/command` 与 `ot/` 已删除，因为服务端不解释文档内容，命令式的插入/删除没有落点。

## 操作流程图

对应实现：`DocEditor.vue` → `collab/otClient.js` → `DocumentWebSocketHandler` → `DocumentService.appendStepBatch`。
服务端不解释文档内容，只负责定序；一致性由客户端的 step 映射保证。

```mermaid
sequenceDiagram
    participant User as 用户A
    participant EdA as 编辑器A(otClient)
    participant WS as WebSocket服务
    participant Svc as 文档服务(定序器)
    participant DB as 数据库
    participant EdB as 编辑器B

    User->>EdA: 输入（ProseMirror 已在本地应用 step）
    EdA->>EdA: 折叠进未发送批次，至多一个在途批次
    EdA->>WS: STEP_BATCH{baseVersion, clientId, steps[]}
    WS->>WS: 握手期由 token 得到身份；documentId 取自路径
    WS->>Svc: appendStepBatch(...)
    Svc->>DB: SELECT ... FOR UPDATE（文档行锁）
    Svc->>Svc: 用锁到的行校验写权限
    alt baseVersion == 当前 version
        Svc->>DB: CAS update version = base+1 where version = base
        Svc->>DB: INSERT operation_log(STEPS, seq)
        Svc-->>WS: 提交后发布 ContentAppliedEvent
        WS->>EdA: ACK{clientId, seq, checkpointRequested}（先单播）
        WS->>EdB: STEPS{seq, steps}（广播，不含来源会话）
        EdB->>EdB: integrate()：入站 step 映射过本地未确认 step 后落地
    else 基版本过期
        WS->>EdA: REJECT{version}
        EdA->>Svc: GET /operations?after= 补齐空洞
        EdA->>EdA: 双向重映射后重发
    end
```

每 `CHECKPOINT_EVERY`（200）个版本，`ACK.checkpointRequested` 让已确认的客户端上传整篇 doc JSON 到
`POST /{id}/checkpoint`，服务端落 `document_snapshot` 并折叠其下方的日志。新加入或重连的客户端拿到的
是"最新检查点内容 + `checkpointVersion`"，再重放其上的日志才能追平。

## 备忘录模式详细图

```mermaid
graph TB
    subgraph "备忘录模式"
        A[Originator<br>文档对象] --> B[Memento<br>备忘录]
        C[Caretaker<br>负责人] --> B
        C --> D[历史栈]
    end
    
    subgraph "操作流程"
        E[执行命令] --> F[保存快照]
        F --> G[更新文档]
        G --> H{需要撤销?}
        H -->|是| I[从备忘录恢复]
        H -->|否| J[继续操作]
    end
    
    A --> E
    I --> A
    
    style A fill:#f3e5f5
    style B fill:#e8eaf6
    style C fill:#e0f2f1
    style D fill:#fff3e0
```

## 观察者模式详细图

一个文档只有**一个** `WebSocketObserver`（按文档引用计数挂/摘，会话数不直接对应观察者数），因此一次变更
只会产生一条广播，而不是"会话数 × 会话数"。发送者按会话排除（不是按用户），同一账号开两个标签页仍会收到。

```mermaid
graph TB
    subgraph "服务端"
        S1[DocumentService<br>提交事务后发布 ContentAppliedEvent]
        S2[DocumentSubjectImpl<br>@TransactionalEventListener AFTER_COMMIT]
        S3[WebSocketObserver<br>每文档一个]
        S4[WebSocketSessionManager<br>ConcurrentWebSocketSessionDecorator]
    end

    subgraph "会话"
        C1[用户A 会话1]
        C2[用户A 会话2]
        C3[用户B 会话1]
    end

    S1 --> S2 --> S3 --> S4
    S4 -->|本节点投递·排除来源会话| C2
    S4 --> C3
    S4 -.->|ACK 单发给来源| C1
```

事务回滚时监听器不触发，未提交的内容不会到达任何客户端；并发写之间帧序不保证，因此**每一帧都带 seq**，
客户端靠序列空洞检测回补。

## 模式协作详细图

```mermaid
graph TB
    subgraph "客户端（协同逻辑）"
        K1[TipTap/ProseMirror 事务] --> K2[otClient 批次折叠]
        K2 --> K3[integrate 双向 step 映射]
        K1 --> K4[编辑器内 history 撤销/重做<br>按用户天然隔离]
    end

    subgraph "定序层（服务端）"
        W[DocumentWebSocketHandler<br>身份取自握手 token] --> AP[appendStepBatch]
        AP --> LK[行锁 PESSIMISTIC_WRITE + 权限校验]
        LK --> CAS[version CAS，基版本过期→ConflictException]
        CAS --> LOG[(operation_log<br>STEPS 顺序日志)]
    end

    subgraph "状态与历史（备忘录）"
        LOG --> CK[每 200 版请求检查点]
        CK --> SNAP[(document_snapshot<br>每文档保留 50 份)]
        SNAP --> RS[restoreVersion<br>旧内容→新版本号]
        RS --> W
    end

    subgraph "通知（观察者）"
        CAS --> EV[ContentAppliedEvent<br>AFTER_COMMIT]
        EV --> OB[每文档一个 WebSocketObserver]
        OB --> K3
    end

    K3 -.->|"已映射的 step 落地文档"| K1
```

注：图中的"命令"角色由客户端的 step 批次承担，服务端不解释内容。原 `pattern/command` 与 `ot/` 已删除
（理由见设计文档"为什么不用字符偏移 OT 处理富文本"）。

## 技术架构图

```mermaid
graph TB
    subgraph "表现层"
        A[Vue 3 + Vite SPA]
        A1[TipTap 编辑器组件]
        A2[协同客户端 otClient]
        A3[历史记录/分享面板]
    end
    
    subgraph "通信层"
        B[WebSocket /ws/document/&#123;id&#125;]
        B1[STEP_BATCH 上行]
        B2[STEPS/ACK/REJECT/RESET 下行]
        B3[CURSOR_UPDATE 与 NOTIFICATION]
    end
    
    subgraph "业务逻辑层"
        C[Spring Boot后端]
        C1[文档控制器]
        C2[用户控制器]
        C3[通知与管理控制器]
    end
    
    subgraph "核心服务层"
        D[领域服务]
        D1[DocumentService<br>定序：行锁 + version CAS]
        D2[MementoCaretaker<br>检查点，保留 50 份]
        D3[DocumentSubject<br>每文档一个观察者]
    end
    
    subgraph "一致性策略"
        E[协同一致性]
        E1[服务端定序 + 客户端双向 step 映射]
        E2[CRDT 为演进方向，未实现]
    end
    
    subgraph "数据层"
        F[(MySQL)]
        F1[document表]
        F2[user表]
        F3[operation_log表]
        F4[document_snapshot表]
    end
    
    A --> B
    B --> C
    C --> D
    D --> E
    E --> F
    
    A1 --> B1
    A2 --> B3
    A3 --> B2
    
    C1 --> D1
    C2 --> D3
    C3 --> D2
    
    D1 --> E1
    D2 --> E1
    D3 --> E1
    
    E1 --> F1
    E1 --> F3
    E2 --> F4
    
    style A fill:#e1f5fe
    style B fill:#e8eaf6
    style C fill:#f3e5f5
    style D fill:#e8f5e8
    style E fill:#fff3e0
    style F fill:#e0f2f1
```

## 数据库ER图

```mermaid
erDiagram
    USER ||--o{ DOCUMENT : creates
    USER ||--o{ OPERATION_LOG : performs
    USER ||--o{ DOCUMENT_SHARE : receives
    USER ||--o{ NOTIFICATION : receives
    DOCUMENT ||--o{ OPERATION_LOG : has
    DOCUMENT ||--o{ DOCUMENT_SNAPSHOT : has
    DOCUMENT ||--o{ DOCUMENT_SHARE : grants
    DOCUMENT {
        bigint id PK
        varchar title
        longtext content "最新检查点内容"
        varchar content_format "html | doc-json"
        int version "协同序列号，只增不减"
        bigint revision "JPA @Version"
        bigint created_by FK
        datetime created_at
        datetime updated_at
    }
    USER {
        bigint id PK
        varchar user_code UK "8位分享码"
        varchar username UK
        varchar email UK
        varchar password_hash "BCrypt，旧 SHA-256 登录时就地升级"
        varchar role "ADMIN | USER"
        bit enabled "停用后登录403、已有token 401"
        datetime created_at
    }
    OPERATION_LOG {
        bigint id PK
        bigint document_id FK
        bigint user_id FK
        varchar command_type "STEPS | SAVE | RESTORE"
        json command_params "含 steps 原文"
        int version UK "与 document_id 联合唯一"
        datetime created_at
    }
    DOCUMENT_SNAPSHOT {
        bigint id PK
        bigint document_id FK
        longtext content
        varchar content_format
        int version UK "与 document_id 联合唯一"
        datetime created_at
    }
    DOCUMENT_SHARE {
        bigint id PK
        bigint document_id FK
        bigint user_id FK
        varchar permission "READ_WRITE | READ_ONLY"
        bigint shared_by FK
        datetime created_at
    }
    NOTIFICATION {
        bigint id PK
        bigint user_id FK
        bigint document_id
        varchar type
        varchar message
        bit read_flag
        datetime created_at
    }
```

`(document_id, version)` 在两张子表上都是唯一约束：它是"序列号不会被铸造两次"这条不变量的数据库级保证。

## 部署架构图

### 当前实际拓扑（可运行）

```mermaid
graph LR
    A[浏览器<br/>Vue3 SPA] -->|/api 与 /ws 代理| B[Vite dev :5173]
    B --> C[Spring Boot 单实例 :8080]
    C --> D[(MySQL collabdoc)]
    C --> E[会话注册表：默认在本进程内存<br/>COLLAB_BUS=redis 时在 Redis]
    C -.->|仅 COLLAB_BUS=redis| R[(Redis 广播 + 在线态)]
```

广播与在线态已经抽象成 `CollabBus`：默认实现 `LocalCollabBus` 只看得见本进程的 socket；`COLLAB_BUS=redis`
时换成 `RedisCollabBus`，帧经 `collab:frames` 发给所有节点、成员表放在 Redis 并由各节点心跳续租，这样在
node A 定序出来的内容才能到达挂在 node B 上的浏览器。**这条外置路径只有代码，没跑过双实例实测**（本机起
Docker Desktop 会触发校园网认证的多网卡拦截，详见 `AGENTS.md`）。

**定序本身一直是集群安全的**：序列号由数据库行锁 + `where version = :base` 的 CAS 铸造，不依赖任何进程内
状态；编辑器内的撤销/重做是前端 ProseMirror history，按用户天然隔离。所以多实例缺的从来不是并发正确性，
而是投递。检查点历史（`document_snapshot`）本来就在数据库里，跨重启与跨实例都成立。

### 目标拓扑（尚未实现，勿当作现状）

下图的消息队列、MySQL 主从、Nginx 负载均衡与 Prometheus/Grafana/ELK 监控均为规划项，`backend/pom.xml`
中没有对应依赖与配置。Redis 是唯一已经落地的：`spring-boot-starter-data-redis` 已引入，用于上面那条
`COLLAB_BUS=redis` 的广播总线；图中的"Redis 缓存"（内容缓存）仍属规划。

```mermaid
graph TB
    subgraph "客户端"
        A[Web浏览器]
        A1[移动端]
        A2[桌面端]
    end
    
    subgraph "前端服务器"
        B[Nginx]
        B1[静态资源]
        B2[负载均衡]
    end
    
    subgraph "后端集群"
        C[Spring Boot实例1]
        D[Spring Boot实例2]
        E[Spring Boot实例3]
    end
    
    subgraph "消息队列"
        F[RabbitMQ/Redis]
        F1[操作队列]
        F2[通知队列]
    end
    
    subgraph "数据库集群"
        G[(MySQL主库)]
        H[(MySQL从库)]
        I[(Redis缓存)]
    end
    
    subgraph "监控系统"
        J[Prometheus]
        K[Grafana]
        L[ELK Stack]
    end
    
    A --> B
    A1 --> B
    A2 --> B
    B --> B1
    B --> B2
    B2 --> C
    B2 --> D
    B2 --> E
    
    C --> F
    D --> F
    E --> F
    F --> F1
    F --> F2
    
    C --> G
    D --> G
    E --> G
    G --> H
    G --> I
    
    C --> J
    D --> J
    E --> J
    J --> K
    J --> L
    
    style A fill:#e1f5fe
    style B fill:#e8eaf6
    style C fill:#f3e5f5
    style D fill:#f3e5f5
    style E fill:#f3e5f5
    style F fill:#fff3e0
    style G fill:#e8f5e8
    style H fill:#e8f5e8
    style I fill:#e0f2f1
    style J fill:#fce4ec
    style K fill:#fce4ec
    style L fill:#fce4ec
```
